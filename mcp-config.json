{
  "//": "Copy this to ~/.copilot/mcp-config.json on the machine that runs germany_watch_research_copilot.py",
  "//1": "Playwright MCP gives the agent a real browser — required for the /germany-watch-research skill to actually search Google, Gelbe Seiten, Northdata etc.",
  "//2": "Install Playwright browsers once: npx playwright install chromium",
  "mcpServers": {
    "playwright": {
      "type": "local",
      "command": "npx",
      "args": ["@playwright/mcp@latest"]
    }
  }
}
